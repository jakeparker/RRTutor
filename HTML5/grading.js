function processIncorrect(selection, action, input) {
  switch (action) {
    case '':
      break;
  }
}

function processCorrect(selection, action, input) {
  switch (action) {
    case 'UpdateComboBox':
      break;
  }
}
